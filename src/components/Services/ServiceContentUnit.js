

function ServiceContentUnit(service) {
    // input service is a string

    // loop ServiceContentList to find service (find)
    // retrieve the object from list
    // display using html

}

// ServiceContentList.find(func returns a boolean (if statement))
// - arrow functions
// - find() function in js

// navbar
// x, y, z
// x onClick() => setSelectedService(string)
// setSelectService() => useState React (string)
// <div>ServiceContentUnit(selectedServiceState)</div>
// find the given service in the ServiceContentList ->
// return ( HTML )
// HTML -> how we want the content to be displayed
// <h1>{service.title}</h1>
// <p>{service.long_desc}</p>